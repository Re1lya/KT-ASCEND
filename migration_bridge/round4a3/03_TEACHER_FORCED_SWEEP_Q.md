# Corpus Q Teacher-Forced Sweep

Evidence state: **A3_VERIFIED**.

The final accepted capture contains 576 All-NPU and 576 P1 Hybrid positions.
Every request used the frozen All-NPU greedy prefix, requested one token, and
recorded the actual serving token, full 102,400-entry FP32 logit vector, top-16,
and Layer 17 route evidence.

## Instrumentation closure

Two early captures were rejected before contract derivation:

1. two accidentally concurrent All-NPU sweep processes interleaved dump files;
2. the first selector chose the input-logprob pass rather than the serving
   sampling pass generated by `return_logprob`.

The final tool holds an exclusive output-directory lock, waits for both forward
passes, selects `positions == len(prefix)`, and requires the HTTP serving token
to have zero logit gap from the captured maximum. Final manifests:

- All-NPU: 576 rows, SHA `f01706aecad98808ab0087c0067c76b99a9477cf8ead288ae0d0fbef974ed6d4`
- Hybrid: 576 rows; see `evidence/manifests/q-hybrid.json`
- maximum All-NPU response-to-max gap: 0
- all logits finite: yes

Invalid intermediate metrics were not used by the derivation.
