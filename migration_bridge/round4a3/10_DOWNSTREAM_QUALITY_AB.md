# Downstream Quality A/B

Status: `NOT_RUN_HELDOUT_BLOCKED`.

The frozen GSM8K and C-Eval manifests remain available, but quality A/B is
downstream of held-out numerical approval. Running it could not override the
contract failure and was therefore skipped under the ordered-stop rule.
