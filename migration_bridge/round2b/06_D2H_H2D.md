# D2H and H2D Verification

All copies used PyTorch `non_blocking=True` on a real Ascend stream with pinned CPU storage.

| Direction | dtype | Ordering proof | Correctness |
|---|---|---|---|
| D2H | BF16 | callback marker after copy | exact |
| D2H | int64 | callback marker after copy | exact |
| D2H | float32 | callback marker after copy | exact |
| H2D | BF16 | subsequent NPU arithmetic reads result | exact |

The invalid-buffer paths reject pageable CPU memory and device memory where pinned host memory is required. The focused transfer module reported `5 passed in 6.89s`.

Evidence: `ascend-transfers.log`; tests: `test_ascend_transfers.py`.
